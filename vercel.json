{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pip install -r requirements.txt --break-system-packages && python scripts/build_chat_index.py && mkdocs build",
  "outputDirectory": "site",
  "framework": null,
  "functions": {
    "api/chat.py": {
      "maxDuration": 60
    }
  }
}
